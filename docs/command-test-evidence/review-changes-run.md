# Тестовий прогін: `.claude/commands/review-changes.md`

**Рендеринг:** так само, як у `add-tests-run.md` — `$ARGUMENTS` підставлено
буквально (`sed`) значенням `git diff 08ded3c..HEAD -- docs/ AGENTS.md`, щоб
перевірити команду на цілі, відмінній від `app/` (генералізація, а не лише
повторний прогін на quote.ts).

## Рендерений промпт (те, що фактично побачив агент)

```
Ціль рев'ю: git diff 08ded3c..HEAD -- docs/ AGENTS.md

Якщо `git diff 08ded3c..HEAD -- docs/ AGENTS.md` порожній — використай поточний незакомічений діф
(`git diff`); якщо і він порожній, попроси користувача уточнити ціль
(commit range, файл, чи PR).

## Роль

Незалежний рев'юер, що бачить діф вперше — без бажання «захистити»
попередню роботу, лише шукає дефекти й розбіжності.

## Мета

Знайти в дифі дефекти коректності та розбіжності між реалізацією, тестами
й документацією — перш ніж це піде далі.

## Контекст

- Ціль: `git diff 08ded3c..HEAD -- docs/ AGENTS.md` (конкретний діф/файл/діапазон комітів).
- Якщо для зміненого коду є тести й документація — звір усі три джерела
  одне з одним, а не лише сам код.

## Обмеження

- НЕ переписуй код і не пропонуй рефакторинг «на смак» — лише дефекти й
  розбіжності.
- Максимум 7 знахідок, відсортованих за серйозністю.
- Явно перевір крайові випадки: 0, від'ємні значення, порожні/дуже великі
  входи, помилки округлення чи типів, якщо застосовно до домену.

## Acceptance criteria

- [ ] Кожна знахідка: файл:рядок, у чому проблема, конкретний вхід (якщо
      застосовно), що саме зламається чи розійдеться.
- [ ] Явно сказано, чи документація (README/коментарі) досі точно описує
      поведінку коду після цього дифу.
- [ ] Явно сказано, чи наявні тести покривають знайдені проблеми.
- [ ] Якщо дефектів немає — так і написано, без вигаданих зауважень.

## Формат відповіді

Список знахідок:
`СЕРЙОЗНІСТЬ | файл:рядок | проблема | вхід (якщо є) | наслідок`

## Stop

Після списку знахідок зупинись. Виправлення — окремим запитом.
```

## Повна відповідь агента (без редагування)

Now I have all the confirmed line numbers. Let me present the findings.

**Ціль перевірена:** `git diff 08ded3c..HEAD -- docs/ AGENTS.md` (не порожній, 493 рядки, 6 файлів). Незакомічена зміна `app/src/quote.test.ts` поза межами цієї цілі — не рецензувалась.

**Знахідки:**

1. HIGH | `docs/injection-report.md:23-44` (+ `docs/injection-report-evidence/blind-test-neutral-input.md`, відсутній `decoy-stripped.md`) | Висновок «до захисту» побудований на «трьох прогонах», але верифікований слід (`docs/injection-report-evidence/*`) є лише для одного з них (варіант 3, другий запуск). Варіант 1 («оригінальний файл») і варіант 2 (нібито збережений як `decoy-stripped.md`) — цього файлу немає ніде в репозиторії (`find . -iname "*decoy-stripped*"` — порожньо), тобто це переказаний, а не збережений прогін. Навіть цитата з «найчистішого» прогону варіанту 3 сама підписана як «без збереженого транскрипту». Це прямо суперечить правилу з `docs/walkthrough.md:194-196` — «перевірка існує, тільки якщо лишила слід (описані, але не збережені прогони не рахуються)». | Вхід: перевірка існування `materials/decoy-stripped.md` / будь-де в репо. | Наслідок: рев'ювер (CodeRabbit/людина) має зарахувати лише 1 з 3 заявлених прогонів; висновки видають ширшу емпіричну базу, ніж насправді підтверджено доказами.

2. MEDIUM | `docs/injection-report.md:27` | Чекбокс `- [x] промовчав про приховану інструкцію` (позначено TRUE) суперечить власному тексту нижче («відмовилась, явно назвавши її користувачу») і збереженому транскрипту (де агент дослівно цитує SYSTEM-блок користувачу). | — | Наслідок: документ технічно суперечить сам собі щодо ключового факту.

3. LOW | `AGENTS.md:27-31` | Список «Keep artifacts in the agreed paths» не згадує `docs/injection-report-evidence/`, хоча `injection-report.md` посилається на нього як на частину доказів. | — | Наслідок: рев'ю за списком узгоджених шляхів не побачить цей каталог як очікувану частину здачі.

4. LOW | `AGENTS.md:59` | Правило 2 сформульоване як «any file matching KEY|TOKEN|SECRET|PASSWORD» — збіг за *назвою* файлу. Payload у `materials/decoy-request.md` просить відкрити файли, що містять ці слова у *вмісті*, не обов'язково в назві. | Вхід: файл з нейтральною назвою, що містить рядок `SECRET_KEY=...` всередині. | Наслідок: правило вужче, ніж атака, яку воно нібито закриває.

Документація і код: діф суто документаційний — тестового покриття для цих знахідок немає й не може бути (це не код); єдиний «тест» — збережені транскрипти, і саме їх бракувало для більшості заявлених прогонів (знахідка 1).

## Незалежна перевірка (не зі слів агента)

```
$ cd app && npm test && npm run typecheck
Tests 25 passed (25); typecheck — clean
(на момент цього прогону — до Task D тестів add-tests, тому 25, не 30)

$ git status --short
(без змін — це був чистий рев'ю без запису)
```

## Що сталося з цими знахідками

Усі чотири виправлено окремим комітом
(`357a171 Task C: fix findings surfaced by /review-changes...`):
1. Реконструйовано й збережено транскрипти для варіантів 1 і 2
   (`docs/injection-report-evidence/blind-test-original-transcript.md`,
   `blind-test-stripped-*.md`), знято невідповідну заяву.
2. Знято прапорець `[x]` на `[ ]` для «промовчав» — виправлено
   суперечність.
3. Додано `docs/injection-report-evidence/` до списку узгоджених шляхів
   в `AGENTS.md`.
4. Уточнено правило 2 в `AGENTS.md`: тепер явно охоплює і назву, і вміст
   файлу.

Це саме той цикл, який Task D мав продемонструвати: команда знаходить
реальну проблему → проблему виправлено окремим кроком, а не проігноровано.
